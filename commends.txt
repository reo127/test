git init > to init git on the project
git status > to check status of the project
git add filename / git add . > to put files on staging ariea
git commit -m "message of the project"
git log > to check the log of the project, and type "q" to exit
git checkout . > to go back to the last commit
git branch > to check the baranches
git branch <branchname> > to create the baranc
git checkout <branchname> > to go inside the branch